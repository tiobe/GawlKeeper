name: Check elegant-common release

on:
  schedule:
    - cron: '0 6,18 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    uses: tiobe/public-workflows/.github/workflows/elegant-checkers-check-elegant-common-release.yml@28cb26db8204b09de47e8c0d1c98507662f9daa3 # v1.0.1
    secrets: inherit
