name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      ref: ${{ github.event.release.tag_name }}
      package: true
    secrets: inherit

  publish:
    needs: build
    uses: tiobe/workflows/.github/workflows/release-publish.yml@vN
    with:
      product-name: GawlKeeper
      semver: ${{ needs.build.outputs.semver }}
    secrets: inherit
