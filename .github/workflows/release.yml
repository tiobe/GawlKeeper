name: Release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      ref: ${{ github.event.release.tag_name }}
      package: true
    secrets: inherit

  publish:
    needs: build
    uses: tiobe/public-workflows/.github/workflows/release-publish.yml@28cb26db8204b09de47e8c0d1c98507662f9daa3 # v1.0.1
    with:
      product-name: GawlKeeper
      semver: ${{ needs.build.outputs.semver }}
    secrets: inherit
